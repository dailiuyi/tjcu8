# tjcu8

A five-page static website built with Vite and deployed to GitHub Pages and `tjcu8.elma-gohan.xyz`.

```powershell
npm ci
npm run dev
npm run build
npm run check:dist
```

See `docs/refactoring-progress.md` for the incremental frontend learning path and `docs/vite-toolchain.md` for the current build contract.