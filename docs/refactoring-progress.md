# 前端渐进式重构进度

更新时间：2026-08-30

## 协作职责

Codex 在本仓库中担任前端重构教练和实施者：先用 Spring 类比解释模块职责边界，再列出涉及文件、约束和验收标准；以一个页面或完整功能模块为单位推进，模块内相关结构、样式、行为和检查一起完成；完成后报告实际验证结果、更新本文档，并明确指出下一个模块。只有跨技术阶段或存在高风险决策时才继续拆小。不要把未完成的练习描述为已掌握，也不要在未确认时引入 Vue、组件库或额外构建工具。

## 长期约束

- 保留现有中文玩梗文案和站点人格，除非任务明确要求修改内容。
- `index.html` 是唯一正式主页，不恢复 `demo.html`。
- 页面结构、样式、行为和数据生成脚本应各自承担单一职责。
- 所有改动先保护现有未提交内容，再进行本地 HTTP、语法和引用验证。
- 不向前端、日志或文档写入密钥和私人数据。
- 正式部署需要兼顾 GitHub Pages，以及绑定已备案域名的阿里云个人服务器。
- 本项目的阿里云独立域名确定为 https://tjcu8.elma-gohan.xyz/。
- 用户已授权直接操作服务器，但 https://elma-gohan.xyz/ 根路径正在承载另一套正式网站；未确认子域名或隔离站点目录前，不部署本项目，更不能覆盖现有根站点。
- 密码、SSH 私钥、令牌等凭据只能在受控会话中临时使用，不写入仓库、日志、文档或长期记忆。

## 已完成

1. 统一正式主页为 `index.html`，删除 `demo.html` 并修正所有首页链接。
2. 为五个页面建立唯一的 `header → main → footer` 语义结构。
3. 删除页面内 `<style>`，把图片索引样式迁移到 `css/style.css`。
4. 保留 `services.html` 作为服务列表页；将 `one_services.html` 重命名为 `image-index.html`。
5. 将图片索引页的内联 JavaScript 提取到 `js/image-index.js`。
6. 将图片生成器整理为 `scripts/generate-image-index.py`。
7. 建立图片索引契约 v1，统一使用正斜杠 URL，并更新 JavaScript 消费者。
8. 新增标准库检查器，自动验证页面引用、图片索引契约及索引与本地图片的一致性。
9. 重构图片预览交互，补齐淡出隐藏、加载与失败状态，以及键盘焦点和 Esc 关闭支持。
10. 清理 HTML/CSS 遗留注释、无效规则和重复声明，重新建立只解释关键设计意图的注释。
11. 建立 768px 移动端断点，调整头部导航、内容卡片、图片和服务项宽度，同时保持桌面规则不变。
12. 统一五个页面的页头、主导航和页脚契约，添加唯一的 `aria-current`，并纳入自动检查。
13. 整理关于页内容结构，以具名文字区块和图片区块替代误用类及空段落，并加入结构检查。
14. 完成服务页模块重构：服务项语义化、施工状态分离、桌面与移动端间距保持，并加入模块检查。
15. 完成全站可访问性与元数据模块：统一语言和标题层级、当前导航视觉、图片替代文本、联系页语义及自动检查。
16. 建立 GitHub Actions 质量门禁：Pull Request 和 `main` 推送先检查生成数据、站点契约与仓库空白格式，只有非 PR 事件通过后才打包并发布 GitHub Pages。
17. 完成阿里云隔离部署：建立版本目录与 `current` 原子链接、独立 Nginx 配置和 Let's Encrypt HTTPS，启用自动续期并验证原根站点未受影响。
18. 完成阿里云自动发布：建立无 sudo 的 `tjcu8-deploy` 账号、受校验保护的原子发布脚本和 GitHub `production` Environment；负向校验与真实切换演练均通过。
19. 完成首次真实 GitHub Actions 验收：静态质量门禁、GitHub Pages 发布和阿里云自动发布三条链路全部通过。
20. 完成前端组件化准备：以标准库生成器统一五个页面的页头、导航和页脚，保留完整静态 HTML，并将布局契约接入质量门禁。
21. 完成 CSS 架构模块：以 `style.css` 为稳定入口，将基础、布局、公共组件、页面组件和响应式规则拆分，并把模块顺序接入自动检查。

## 当前技术决策

静态阶段继续使用只依赖标准库的 Python 生成器。图片数据遵循 `docs/image-index-contract.md`，公共页面壳遵循 `docs/shared-layout.md`。CSS 以 `css/style.css` 为唯一入口，按 `docs/css-architecture.md` 的职责顺序加载。浏览器端 JavaScript 不能枚举静态服务器的图片目录；进入 Vue/Vite 后，再评估 Node 脚本或 `import.meta.glob`。质量门禁与部署目标解耦。阿里云使用 `/var/www/tjcu8/releases/<release-id>` 与 `current` 软链接原子发布，细节见 `docs/deployment.md`。

## 下一步

进入图片索引 JavaScript 架构模块：按数据加载、列表渲染和预览交互拆分职责，保持现有 JSON 契约、文案、键盘行为和视觉不变。
